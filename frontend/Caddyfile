# Caddyfile for the React SPA container
# Serves static files from /srv with HTML5 history-mode fallback
{
    auto_https off
    admin off
}

:80 {
    root * /srv
    encode gzip

    # SPA fallback – serve index.html for all non-file routes
    try_files {path} /index.html
    file_server
}
